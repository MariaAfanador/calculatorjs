const sumTotal = (a, b, c) => {
  return a + b + c;
};

const subtTotal = (a, b, c) => {
  return a - b - c;
};

const multiplyByTotal = (a, b, c) => {
  return a * b * c;
};

const divideByTotal = (a, b, c) => {
  return a / b / c / d / e;
};

const total = sumTotal(5, 6, 7);
console.log(total);
